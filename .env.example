# Frontend env vars (Vite). Real values go into Cloudflare Pages
# environment variables — never commit secrets to this repo.

# Public API base — leave empty for same-origin (Cloudflare Pages Functions at /api/*)
VITE_API_BASE=""
